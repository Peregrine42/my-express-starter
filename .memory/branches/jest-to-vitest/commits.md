# jest-to-vitest

**Purpose:** Migrate the project from Jest to Vitest: replace jest deps, update configs, rewrite test helpers, eliminate the tsdown test build step, and update E2E to use Puppeteer directly instead of jest-puppeteer.
