#!/usr/bin/env sh

# Only run tests when pushing to master
remote="$1"
while read local_ref local_sha remote_ref remote_sha; do
  case "$remote_ref" in
    refs/heads/master)
      echo "Pushing to master — running tests..."
      npm test || exit 1
      ;;
  esac
done
